services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_BACKEND_URL=http://localhost:8000
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - VITE_BACKEND_URL=http://localhost:8000